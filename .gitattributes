# Normalize line endings in the repository; check out with the platform's native ending.
* text=auto

# Binary assets Git must never try to diff or convert.
*.png binary
*.dll binary
*.pdb binary
*.rar binary
*.zip binary
